# Import the Path class from the pathlib module
from pathlib import Path

# Define file paths for configuration and parameters YAML files using the Path class
CONFIG_YAML_FILE = Path(r'config\configuration.yaml')  # Path to the configuration YAML file
PARAMS_YAML_FILE = Path(r'config\params.yaml')  # Path to the parameters YAML file
